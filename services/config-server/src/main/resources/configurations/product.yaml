spring:
  datasource:
    url: jdbc:postgresql://localhost:5436/product
    username: rafaelswr
    password: 1223
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    baseline-on-migrate: true
    enabled: true
    baseline-description: "init"
    baseline-version: 0
    user: rafaelswr
    password: 1223

server:
  port: 8989